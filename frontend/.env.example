# Copy to .env and fill in the value for your environment.
# For local dev, the default in api.js already falls back to http://127.0.0.1:8000
# For production, set this to your deployed backend URL (no trailing slash).
VITE_API_BASE=https://your-api-domain.com
